namespace TK.Haptics
{
    /// <summary>Notification feedback — maps to iOS UINotificationFeedbackGenerator types and Android patterns.</summary>
    public enum HapticNotification
    {
        Success = 0,
        Warning = 1,
        Error = 2
    }
}
