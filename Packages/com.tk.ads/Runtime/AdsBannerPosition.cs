namespace TK.Ads
{
    /// <summary>Anchor position for the banner ad view (mirrors the AppLovin MAX banner positions).</summary>
    public enum AdsBannerPosition
    {
        TopLeft,
        TopCenter,
        TopRight,
        Centered,
        CenterLeft,
        CenterRight,
        BottomLeft,
        BottomCenter,
        BottomRight
    }
}
