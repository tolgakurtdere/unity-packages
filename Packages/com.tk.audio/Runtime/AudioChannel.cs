namespace TK.Audio
{
    /// <summary>Built-in mixing channels. Named categories are a deliberate non-goal for now.</summary>
    public enum AudioChannel
    {
        Music = 0,
        Sfx = 1
    }
}
