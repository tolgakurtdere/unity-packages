# com.tk.core v0.4.0 — AnimatedTabButtonPresenter Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (inline, per core-release precedent). Binding behavioral contracts live in the approved spec: `docs/specs/2026-07-07-tk-core-animated-tab-presenter-design.md` — READ IT FIRST.

**Goal:** Magic Sort-style selected-tab animation as a dependency-free package presenter (sprite swap + bottom-pivot scale + layout-reflow width + label fade), plus per-tab icons in `TabBarConfig` — additive, zero API break.

**Tech Stack:** Unity 6000.3.6f1 harness, C#, NUnit EditMode. No new dependencies (`Unity.TextMeshPro`/`UnityEngine.UI` already referenced).

## Global Constraints

- Repo `/Users/tolgahankurtdere/Documents/GitHub/unity-packages`, branch `core-v0.4.0` off `main` (`20332f2` = tag `com.tk.core/0.3.1`). **NO ff-merge, NO tag at the end** — both wait for play-mode verification in game-shikaku (spec decision 3). Pins stay 0.3.1 everywhere.
- Zero break: existing 336 tests pass unmodified; `TabButtonData`'s existing ctor and all existing serialized field names stay.
- Harness gate (unchanged command, from `<scratchpad>/tk-verify`; NEVER `-quit` with `-runTests`): success = exit 0, all Passed, zero `error CS`/`warning CS` under `Packages/com.tk.core`. New files get hand-written minimal `.meta`s (`fileFormatVersion: 2` + `guid:`), written together with the `.cs`.
- Conventional commits, trailer `Co-Authored-By: Claude <noreply@anthropic.com>`.

### Task 1: Per-tab icons (config → data → view → default presenter) + tests

**Files:** Modify `Runtime/UI/TabBar/TabBarConfig.cs` (Entry gains `public Sprite icon;` with tooltip), `TabButtonData.cs` (add `using UnityEngine;`, `Sprite Icon { get; }`, new 4-arg ctor; old 3-arg ctor chains to it with `null`), `TabBarView.cs` (pass `tab.icon` into `TabButtonData`), `DefaultTabButtonPresenter.cs` (optional `[SerializeField] Image icon;` + apply `data.Icon` in `Initialize` when both set). Tests: extend `TabBarViewTests` (SerializedObject sets an icon sprite on one entry; a recording test-presenter on the template asserts `data.Icon` arrives) and add `TabButtonData` old-ctor compat assert.

- [ ] Implement + tests. Commit: `feat(core): per-tab icons in TabBarConfig/TabButtonData (additive)`.

### Task 2: AnimatedTabButtonPresenter + meta

**Files:** Create `Runtime/UI/TabBar/AnimatedTabButtonPresenter.cs` (+`.meta`).

Implementation per spec: serialized surface exactly as specified; `VisualState` struct (bg color, text color, label alpha, scale, width); FROM captured from live component values on every `SetSelected`; generation-counter reentrancy; `async void` loop with `LogException` + destroyed checks; sprite swap at start; `protected virtual ApplyProgress(float)` single write point (LerpUnclamped for scale/width, clamped for colors/alpha); default easing curve built in code (`(0,0,→2.4)`, `(0.7,1.06,0,0)`, `(1,1,0,0)`); `Initialize` warnings (pivot ≠ (0.5,0) with `selectedScale>1`; `selectedWidth>0` without `LayoutElement`); width-capture `<0` → `normalWidth`.

- [ ] Implement. Commit: `feat(core): AnimatedTabButtonPresenter — sprite/scale/width/label selected-state animation`.

### Task 3: EditMode tests

**Files:** Create `Tests/Editor/AnimatedTabButtonPresenterTests.cs` (+`.meta`).

Coverage (all frame-free, via a test subclass exposing `ApplyProgress` + `Initialize` with a synthetic Button rig: RectTransform+Image+Button root, child TMP-less label? — label needs TMP: create a child with `TextMeshProUGUI`? NO: tests avoid TMP instantiation (resource flake risk, established earlier); label channel is covered by leaving `label` null in most tests and by one test wiring a plain `TMP_Text`-less path — label-specific asserts (alpha/text) are therefore exercised ONLY if TMP proves stable headless; otherwise leave label null and document. Everything else (sprite, colors, scale, width, warnings, icon) tests without TMP):

- [ ] Instant select/deselect snaps: background color + sprite swap + scale (`localScale`) + `LayoutElement.preferredWidth`.
- [ ] Null sprites → `Image.sprite` untouched.
- [ ] Retarget: after instant-normal, start non-instant select, `ApplyProgress(0)` ≈ normal values, `ApplyProgress(1)` = selected values; then mid-flight (`ApplyProgress(0.5)`) re-`SetSelected(false)` and assert FROM ≈ the mid values (current-capture proof).
- [ ] `ApplyProgress(1.1)` → scale/width overshoot beyond targets; colors stay clamped at endpoint values.
- [ ] Warnings: pivot ≠ (0.5,0) with scale>1 → LogAssert warning; `selectedWidth>0` + no LayoutElement → warning, width writes skipped.
- [ ] Icon applied from `TabButtonData` when icon Image assigned.
- [ ] Gate: 336 + new, all green. Commit: `test(core): AnimatedTabButtonPresenter coverage`.

### Task 4: Docs + version (no pins, no tag)

- [ ] `package.json` → `0.4.0`; CHANGELOG `## [0.4.0] - Unreleased` (Added: presenter with contract summary, per-tab icons; note tag-after-game-verification); core README Tab bar section: presenter bullet + "recommended bar setup" paragraph (HorizontalLayoutGroup lower-center + padding, template pivot (0.5,0), LayoutElement for width). Commit: `chore(core): 0.4.0 unreleased — changelog, readme, version`.
- [ ] Report to user: branch state + how to play-mode verify in shikaku (temporary `file:` override) + that merge/tag awaits that verification.
